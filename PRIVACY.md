# 隐私说明

## 数据在哪里

本项目是纯前端工具，不包含账号、远程脚本、分析埋点或服务器上传逻辑。填写内容会以明文 JSON 保存在当前页面来源对应的浏览器 `localStorage` 中。

## JSON 备份

“导出 JSON”生成的是明文文件，不提供加密。任何获得文件的人都可能直接读取其中内容。

## 使用建议

- 不要把真实资料提交到 Git 仓库、Issue、聊天群或公共网盘。
- 共用设备使用后应点击“恢复初始模板”，并清理浏览器站点数据。
- 不要在不可信网站中嵌入本工具，也不要随意加入第三方脚本。
- 发布前运行 `npm run check`，确认隐私回归测试通过。
- 私人版本只放在已忽略的 `_private/` 或仓库外。

项目内保留的私人原始页面不会进入公开构建。


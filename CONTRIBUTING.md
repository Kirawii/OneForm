# 贡献指南

感谢你改进 OneForm。

## 项目边界

项目的核心是“填写一次，随用随复制”。新增能力应直接改善字段整理、查找、复制、备份或提交核对；避免把首页、社区、申请管理等次要功能放到核心流程之前。

## 源码职责

- `index.html`：语义结构、字段和文案
- `src/styles.css`：视觉样式
- `src/app.js`：本地保存、搜索、复制和导入导出
- `scripts/build.mjs`：生成可离线打开的单文件版本
- `tests/static.test.mjs`：结构和隐私回归检查

保持原生 HTML、CSS、JavaScript，除非新依赖解决了明确且无法用现有方式处理的问题。

## 隐私底线

- 不提交任何真实申请人的姓名、证件号、学号、联系方式、学校经历或备份文件。
- 截图、导出 JSON 和个人定制版本应放在仓库外，或放入已忽略的 `_private/`。
- 示例内容必须明确虚构，且不得由真实个人资料轻微改写而来。
- 不引入远程脚本、埋点、广告或远程字体。

## 提交前

```bash
npm run check
```

若修改字段，请同时确认搜索、复制、导出、导入和完成度统计仍能识别该字段。

# 安全策略

## 支持范围

安全修复进入当前主版本和最新单文件发行版。

## 威胁模型

本项目主要关注个人资料误提交、明文备份泄露、导入异常 JSON、动态内容注入和第三方资源读取本地数据。当前版本无远程依赖，并在动态 HTML 输出前执行转义。

本项目不能防止已解锁设备、恶意浏览器扩展、屏幕读取程序、被篡改的托管副本或用户主动分享资料。

## 报告漏洞

请使用仓库的私密安全报告渠道，不要在公开 Issue 中附带真实姓名、证件号、联系方式、截图或备份文件。报告请包含受影响版本、最小复现步骤、预期与实际行为，以及不含个人数据的示例载荷。

## 发布检查

```bash
npm run check
```

同时确认公开目录中不存在个人 JSON、截图、证件、照片或 `_private/` 内容。

